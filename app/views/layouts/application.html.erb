<!DOCTYPE html>
<html>
  <head>
    <title> <%= content_for :page_title %> | Postmates: Food Delivery, Groceries, Alcohol - Anything from Anywhere</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
    <header>
      <div class="header-wrap container p-3 d-flex flex-wrap align-items-center justify-content-between">
        <h1 class="mb-0">
          <%= link_to root_path do %> 
            Postmates
          <% end %>
        </h1>
        <nav class="d-flex flex-wrap justify-content-between">
          <%= link_to 'LOG IN', root_path, class: 'btn btn-outline-dark mr-3 rounded-pill px-4' %>
          <%= link_to 'SIGN UP', root_path, class: 'btn btn-dark rounded-pill px-4' %>
        </nav>
      </div>
    </header>
    <%= yield %>
  </body>
</html>
